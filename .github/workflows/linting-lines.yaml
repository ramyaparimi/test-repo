# name: Linter for docs contribution PRs
# on:
#   pull_request

# permissions:
#   contents: read
#   pull-requests: write

# jobs:
#   linter:
#     runs-on: ubuntu-latest
#     steps:
#       - name: Checkout
#         uses: actions/checkout@master

#       - name: Vale
#         uses: errata-ai/vale-action@v1.4.3 
#         with:
#           config: https://raw.githubusercontent.com/ramyaparimi/fluffy-chickpea/main/.vale.ini

name: Vale Lint
on: [push]

jobs:
  prose:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Vale
        uses: errata-ai/vale-action@310a5ac41197ffb704515622b4914c72d3abc54b
        env:
          GITHUB_TOKEN: ${{secrets.GITHUB_TOKEN}}
        with:
          files: styles

          onlyAnnotateModifiedLines: true
        env:
          # Required, set by GitHub actions automatically:
          # https://docs.github.com/en/actions/security-guides/automatic-token-authentication#about-the-github_token-secret
          GITHUB_TOKEN: ${{secrets.GITHUB_TOKEN}}
